<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"

    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/ivFilterRecipeIcon"
        android:layout_width="64dp"
        android:layout_height="72dp"
        android:background="@drawable/chip_selected_background"
        android:padding="16dp"
        android:contentDescription="@string/chip_content_description"
        android:src="@drawable/ic_baseline_star_rate_24"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvRecipeFilter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/robotoregular"
        android:textSize="16sp"
        android:layout_marginTop="8dp"
        android:maxLength="10"
        app:layout_constraintEnd_toEndOf="@+id/ivFilterRecipeIcon"
        app:layout_constraintStart_toStartOf="@+id/ivFilterRecipeIcon"
        app:layout_constraintTop_toBottomOf="@+id/ivFilterRecipeIcon"
        tools:text="Todos" />

</androidx.constraintlayout.widget.ConstraintLayout>